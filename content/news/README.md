# News content

This directory is managed through the CMS at `/admin/`.

News entries use the filename pattern `<slug>.<locale>.json`. The website only
renders entries whose `status` is `approved` and which include an approver, an
approval-record reference, and source notes. Do not copy internal or
confidential source material into an entry; record only a short public-safe
description of how the claim was checked.
